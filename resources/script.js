(function($){	 var fnSetSCrollMenu = function(){		var elScroll = $('.zyb-scroll-menu');				elScroll.css({			position: 'fixed',			left:  $('.container.zyb-content').offset().left + 'px',			top: 0		});		var elPageNav =  $('.zyb-page-nav');		var $doc = $(document);		var margin = elPageNav.offset().top + elPageNav.height() / 2;		var flag = true;		$(document).on('scroll', function(e){			if(flag) {				if($doc.scrollTop() >= margin){					elScroll.show();					flag = false;				}			} else {				if($doc.scrollTop() < margin){					elScroll.hide();					flag = true;				}			}		});	}	$(function(){		setTimeout(fnSetSCrollMenu, 500);	});})(jQuery);